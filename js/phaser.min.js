const config = {
  type: Phaser.AUTO,
  parent: "game-root", // <- koppelt aan je div
  width: "100%",
  height: "100%",
  scale: {
    mode: Phaser.Scale.RESIZE,
    autoCenter: Phaser.Scale.CENTER_BOTH,
  },
  scene: [PreloadScene, MainScene],
};

const game = new Phaser.Game(config);
